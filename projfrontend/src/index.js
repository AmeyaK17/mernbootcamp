import React from "react"
import Routes from "./Routes"
import ReactDOM from "react-dom"

ReactDOM.render(<Routes />, document.getElementById("root")); //<Routes /> is the component to be loaded, and "root" is the location at which it will be loaded. So, .render(What?, Where?)
